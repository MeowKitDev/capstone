import { INTERVAL } from '../enum/dashboard/interval.enum';
import { MODE } from '../enum/dashboard/mode.enum';
import { STATUS } from '../enum/dashboard/status.enum';
import { USER_ROLE } from '../enum/user/user-role.enum';

export const USER_ROLE_OPTIONS = Object.values(USER_ROLE);

export const DASHBOARD_MODE_OPTIONS = Object.values(MODE);

export const DASHBOARD_STATUS_OPTIONS = Object.values(STATUS);

export const DASHBOARD_INTERVAL_OPTIONS = Object.values(INTERVAL);
